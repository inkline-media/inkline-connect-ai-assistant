# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo
.~lock.*#

# Composer (not used yet, but reserve)
vendor/
composer.lock

# Build artefacts — keep the plugin shippable as-is
*.zip

# Local WordPress test environments
wp-config-local.php

# Local secrets — never commit
.env
.env.local
.env.*.local
.npmrc

# Node — only used by tooling that pulls icon assets at build time
node_modules/
package-lock.json
